name: CD

# on:
#   workflow_run:
#     workflows: ["CI"]
#     types:
#       - completed

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run tests against'
        type: environment
        required: true
      release:
        description: 'Release'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-20.04
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Find branch
        run: |
          BRANCH_NAME=$(git branch --contains ${{ inputs.release }})
          echo "Release ${{ inputs.release }} belongs to branch $BRANCH_NAME"

      # - uses: cardinalby/git-get-release-action@v1
      #   id: getByTag
      #   env:
      #     GITHUB_TOKEN: ${{ github.token }}
      #   with:
      #     tag: ${{ inputs.release }}
      # - name: Release Info
      #   run: |
      #     echo ${{ toJSON(steps.getByTag.outputs) }}





      # - uses: robinraju/release-downloader@v1.8
      #   with:
      #     tag: ${{ inputs.release }}
      #     fileName: artefacts.zip
          
      # - name: Display structure of downloaded files
      #   run: ls -R
      
      # - name: Echo connection string from secrets
      #   run: echo "${GITHUB_REF} ${{ secrets.DB_CONNECTION_STRING }}"